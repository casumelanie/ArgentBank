const initialState = { email: "", password: "" }
// Test en stockant email et password mais à modifier plus tard car il faudra stocker le token

export default function loginReducer(state = initialState, action) {
    return state
}